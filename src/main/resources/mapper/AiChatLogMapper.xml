<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example1.wms.Mapper.AiChatLogMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_chat_log (
            user_query,
            ai_response,
            model,
            prompt_template,
            duration_ms,
            status,
            error_msg,
            create_time
        ) VALUES (
            #{userQuery},
            #{aiResponse},
            #{model},
            #{promptTemplate},
            #{durationMs},
            #{status},
            #{errorMsg},
            NOW()
        )
    </insert>

</mapper>
