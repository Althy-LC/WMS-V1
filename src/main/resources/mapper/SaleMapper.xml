<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example1.wms.Mapper.SaleMapper">
    <insert id="create">
        insert into wms_sale_out (order_no,source_order_no,warehouse_id,customer,status) values (#{orderNo},#{sourceOrderNo},#{warehouseId},#{customer},#{status})
    </insert>

    <select id="getid" resultType="java.lang.Long">
        select id from wms_sale_out where order_no = #{orderNo}
    </select>

    <select id="getBystatus" resultType="com.example1.wms.POJO.SaleOut">
        select order_no,source_order_no,warehouse_id,customer from wms_sale_out where status = #{status}
    </select>

    <select id="getsale" resultType="com.example1.wms.POJO.SaleOut">
        select id,warehouse_id,version from wms_sale_out where order_no = #{orderNo}
    </select>

    <update id="update">
        update wms_sale_out set version = version + 1 , status = #{status} where id = #{id} and version = #{version}
    </update>

    <select id="getsaleBystatus" resultType="com.example1.wms.POJO.SaleOut">
        select id,order_no,warehouse_id from wms_sale_out where status = #{status}
    </select>

    <select id="getorderNo" resultType="java.lang.String">
        select orderNo from wms_sale_out where id = #{orderId}
    </select>

    <select id="getversion" resultType="java.lang.Integer">
        select version from wms_sale_out where id = #{orderId}
    </select>

    <update id="updates">
        <foreach collection="list" item="item" separator=";">
            update wms_sale_out set version = version + 1 , status = #{item.status} where id = #{item.id} and version = #{item.version}
        </foreach>
    </update>

    <!-- 根据订单号更新销售单状态为复核状态 -->
    <update id="updateToReview">
        update wms_sale_out
        set version = version + 1,
            status = #{status.code}
        where order_no = #{orderNo}
          and version = #{version}
    </update>

    <!-- 获取销售单的明细信息（包含库位信息） -->
    <select id="getDetailsWithLocation" resultType="com.example1.wms.POJO.SaleOutDetail">
        select id, order_id, sku_id, quantity, picked_quantity, location_id
        from wms_sale_out_detail
        where order_id = #{orderId}
    </select>

    <!-- 根据订单号更新状态为出库状态 -->
    <update id="updateToOutbound">
        update wms_sale_out
        set version = version + 1,
            status = #{status.code}
        where order_no = #{orderNo}
          and version = #{version}
    </update>
</mapper>