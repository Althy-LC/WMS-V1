<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example1.wms.Mapper.StockMapper">
    <insert id="addOrUpdate">
        insert into wms_stock
        (warehouse_id, sku_id, total_stock, available_stock, locked_stock)
        values
        <foreach collection="collection" item="item" separator=",">
            (#{item.warehouseId},
            #{item.skuId},
            #{item.totalStock},
            #{item.availableStock},
            #{item.lockedStock})
        </foreach>
        ON DUPLICATE KEY UPDATE
        total_stock = total_stock + VALUES(total_stock),
        available_stock = available_stock + VALUES(available_stock),
        locked_stock = locked_stock + VALUES(locked_stock)
    </insert>

    <select id="getTotal" resultType="java.util.Map">
        select 
            COALESCE(SUM(total_stock), 0) AS total,
            COALESCE(SUM(available_stock), 0) AS available,
            COALESCE(SUM(sale_locked), 0) AS sale_locked,
            COALESCE(SUM(locked_stock), 0) AS locked_stock
        from wms_stock 
        where sku_id = #{skuId}
        <if test="warehouseId != null">
            and warehouse_id = #{warehouseId}
        </if>
    </select>

    <select id="getStock" resultType="com.example1.wms.POJO.Stock">
        select sku_id,total_stock,available_stock,locked_stock from wms_stock where sku_id = #{skuId} and warehouse_id = #{warehouseId}
    </select>

    <update id="salelocked">
        <foreach collection="list" item="item" separator=";">
            update wms_stock set total_stock = total_stock - #{item.quantity} ,sale_stock = sale_stock + #{item.quantity},version = version +1 where warehouse_id =#{warehouseId} and sku_id = #{item.skuId} and version = #{item.version}
        </foreach>

    </update>

    <select id="getstockversion" resultType="java.lang.Integer">
        select version from wms_stock where warehouse_id = #{warehouseId} and sku_id = #{skuId}
    </select>

    <!-- 出库扣减库存：total_stock减，sale_locked减 -->
    <update id="outboundStock">
        <foreach collection="list" item="item" separator=";">
            update wms_stock
            set total_stock = total_stock - #{item.quantity},
                sale_locked = sale_locked - #{item.quantity},
                version = version + 1
            where warehouse_id = #{warehouseId}
              and sku_id = #{item.skuId}
              and version = #{item.version}
        </foreach>
    </update>
</mapper>