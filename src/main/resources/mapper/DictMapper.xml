<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example1.wms.Mapper.DictMapper">
  <select id="getDict" resultType="com.example1.wms.POJO.Dict">
      select dict_type,dict_code,dict_name from dict where dict_type = #{dictType} <if test="!isAdmin">and status = 1</if> order by id
  </select>

  <select id="getDictBycode" resultType="com.example1.wms.POJO.Dict">
      select dict_code,dict_name from dict where dict_type = #{dictType} and dict_code = #{s}
  </select>

  <update id="delete">
      update dict set status = 0 where dict_type = #{dictType} and dict_code = #{dictCode}
  </update>

</mapper>